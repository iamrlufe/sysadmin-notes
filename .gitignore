site/
.venv/
__pycache__/
.DS_Store
.claude/

# Датированные снимки графа знаний: замороженные копии graphify-out/graph.json,
# каждая ~300 КБ и на каждый прогон новая. Прошлые версии графа и так лежат в
# истории git — в индексе это чистое дублирование, которое раздувает клон
# (Cloudflare Pages тянет полную историю через `git fetch --unshallow`).
graphify-out/20*/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
